{
  "name": "lex-serverless-webhook",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "*",
    "@twilio/runtime-handler": "1.3.0",
    "@aws-sdk/client-lex-runtime-v2": "*",
    "axios" : "*"
  },
  "devDependencies": {
    "twilio-run": "^3.5.2"
  },
  "engines": {
    "node": "16"
  }
}